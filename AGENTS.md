# Agents

This repo's agent configuration lives in [`CLAUDE.md`](./CLAUDE.md) under `## Agent skills`,
with supporting detail under `docs/agents/`. That file is the source of truth — keep it
authoritative and read it before acting on agent workflows (issue tracker, triage labels,
domain docs).
